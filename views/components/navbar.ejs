<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Library Management</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarColor02"
    aria-controls="navbarColor02"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor02">
    <% if(user){ %>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item-float-right">
        <a
          href="/user/<%=user._id%>"
          class="nav-link<%= typeof PAGE_PATH != 'undefined' ? ( PAGE_PATH === 'user' ? ' active' : '') : ''%>"
          >Profile</a
        >
      </li>
      <li class="nav-item-float-right">
        <a
          href="/borrowed/<%=user._id%>"
          class="nav-link<%= typeof PAGE_PATH != 'undefined' ? ( PAGE_PATH === 'cart' ? ' active' : '') : ''%>"
          >Borrowed</a
        >
      </li>
      <li class="nav-item-float-right">
        <a
          href="/panel/index"
          class="nav-link<%= typeof PAGE_PATH != 'undefined' ? ( PAGE_PATH === 'panel' ? ' active' : '') : ''%>"
          >Panel</a
        >
      </li>
      <li class="nav-item-float-right">
        <a href="/user/signout" class="nav-link">Logout</a>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0" method="post" action="/search">
      <input
        class="form-control mr-sm-2"
        type="text"
        name="text"
        placeholder="Search"
      />
      <button class="btn btn-outline-secondary my-2 my-sm-0" type="submit">
        Search
      </button>
    </form>
    <% } else{ %>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link active" href="/"
          >Home <span class="sr-only">(current)</span></a
        >
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user/signup">Sign-up</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user/signin">Sign-in</a>
      </li>
    </ul>
    <% } %>
  </div>
</nav>
